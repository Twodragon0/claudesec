---
description: Build and serve the ClaudeSec security dashboard
---
Build and serve the ClaudeSec dashboard:
1. Run `./scripts/quick-start.sh` to build scanner image and generate dashboard
2. If Docker is not available, run `./scanner/claudesec dashboard --serve`
3. Open http://localhost:11777/ in the browser
4. Show the dashboard URL to the user
