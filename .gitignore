# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Node (if scripts are added later)
node_modules/
package-lock.json

# Environment
.env
.env.*
.omc/

# Local scanner config (may contain company paths, kubeconfig paths, profile names)
.claudesec.yml
.claudesec.local.yml

# Scan results (never commit scan reports)
claudesec-dashboard.html
*.scan-report.html
*.scan-report.json
docs/reports/scan-report*.md
scanner/output/
.claudesec-prowler/
.claudesec-history/

# Credentials & keys
*.pem
*.key
*.p12
*.pfx
*.secret
credentials.json

# --- Do not upload to GitHub: internal data, PII, IPs, accounts ---
# Local overrides (may contain paths, accounts, internal IPs)
*.local.yml
*.local.yaml
# Kubeconfig and cluster access (paths, account IDs)
kubeconfig
.kube/
*kubeconfig*
# Directories that may hold secrets or internal data
secrets/
credentials/
# Backup/temp (may contain pasted secrets or PII)
*.bak
*.backup
*~
# Common secret/config names
*.secret.yml
*.secret.yaml
config.local.*
