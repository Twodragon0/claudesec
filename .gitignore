# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Node (if scripts are added later)
node_modules/
package-lock.json

# Environment
.env
.env.*
.omc/

# Local scanner config (may contain company paths, kubeconfig paths, profile names)
.claudesec.yml
.claudesec.local.yml

# Scan results (never commit — may contain PII, internal paths, findings)
claudesec-dashboard.html
claudesec-dashboard.*.html
index.html
*.scan-report.html
*.scan-report.json
docs/reports/scan-report*.md
scanner/output/
.claudesec-prowler/
.claudesec-history/
.claudesec-network/
.claudesec-datadog/
.claudesec-datadog/*-sanitized.json
.claudesec-datadog/datadog-logs-sanitized.json
.claudesec-datadog/datadog-cloud-signals-sanitized.json
.claudesec-datadog/datadog-cases-sanitized.json
.claudesec-audit-points/
.claudesec-ms-best-practices/
scan-report.json

# Credentials & keys
*.pem
*.key
*.p12
*.pfx
*.secret
credentials.json

# Python (no commit)
__pycache__/
*.pyc
*.pyo
test-reports/

# --- Do not upload to GitHub: 개인정보·회사정보·PII·IP·계정 (no PII, company info, IPs, accounts) ---
# Local overrides (may contain paths, accounts, internal IPs)
*.local.yml
*.local.yaml
# Kubeconfig and cluster access (paths, account IDs)
kubeconfig
.kube/
*kubeconfig*
# Directories that may hold secrets or internal data
secrets/
credentials/
# Backup/temp (may contain pasted secrets or PII)
*.bak
*.backup
*~
tmp-*.svg
tmp-*.png
# Common secret/config names
*.secret.yml
*.secret.yaml
config.local.*
