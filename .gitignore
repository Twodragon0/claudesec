# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Node
node_modules/

# Python
__pycache__/
*.pyc
*.pyo
.venv*/
venv*/
test-reports/

# Environment & Secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.secret
*.secret.yml
*.secret.yaml
*.local.yml
*.local.yaml
config.local.*
credentials.json
authorized_user.json
secrets/
credentials/
kubeconfig
.kube/
*kubeconfig*

# Scanner config (may contain local paths)
.claudesec.yml
.claudesec.local.yml

# Scan results & generated dashboards (contain findings, internal paths)
claudesec-dashboard.html
claudesec-dashboard.*.html
claudesec-asset-dashboard-live.html
index.html
scan-report.json
*.scan-report.html
*.scan-report.json
scanner/output/
docs/reports/scan-report*.md

# Data directories (contain PII, hostnames, IPs, account IDs)
.claudesec-assets/
.claudesec-prowler/
.claudesec-history/
.claudesec-network/
.claudesec-datadog/
.claudesec-audit-points/
.claudesec-ms-best-practices/
.claudesec-saas-best-practices/
.claudesec-zscaler/

# Local data files (employee names, serial numbers, pricing)
jamf-*.json
jamf-*.csv
gsheet-*.json
aws-*.json
prowler-*.json
notion-*.json

# Local run script (contains environment-specific paths)
run-all.sh

# Temp
*.bak
*.backup
*~
tmp-*.svg
tmp-*.png

# MCP / Orchestration (local tool configs)
.mcp.json
.omc/
.omx/
scanner/lib/.omc/
.playwright-mcp/

# Docker logs (may contain internal paths)
docker-full.log
docker-*.log

# Screenshots (may contain internal dashboards)
isms-*.png
main-*.png
main-snapshot.md

# Internal documents (seminar, architecture PDFs — may contain company info)
docs/seminar/
docs/architecture/*.pdf

# Claude Code (local settings and hooks — agents and commands are shared)
.claude/settings.json
.claude/settings.local.json
.claude/hooks/

# Summary/presentation files (internal, not for public repo)
claudesec-summary-*.md
claudesec-summary-*.pptx
claudesec-summary-*.pdf

# Architecture diagrams (auto-generated, may contain scan-specific data)
docs/architecture/*.svg
docs/architecture/*.drawio
