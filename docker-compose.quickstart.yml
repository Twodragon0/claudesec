# ClaudeSec Quick Start — One-command security scan + dashboard
#
# Usage:
#   docker compose -f docker-compose.quickstart.yml up
#   open http://localhost:11777
#
# This file is designed for first-time users who want to:
#   1. Scan their project for security issues
#   2. View results in an interactive dashboard
#   3. No configuration needed
#
# Note: claudesec-asset-dashboard-live.html is intentionally mounted here because
# dashboard-gen generates it before the dashboard service starts. This is safe in
# the quickstart flow but that file is NOT mounted in docker-compose.yml (where it
# may not exist on a fresh clone without API keys).

services:

  scan:
    build: { context: ., dockerfile: Dockerfile }
    image: claudesec:local
    command: ["scan", "-d", "/workspace", "-c", "all"]
    volumes: [".:/workspace"]
    working_dir: /workspace

  dashboard-gen:
    build: { context: ., dockerfile: Dockerfile }
    image: claudesec:local
    command: ["dashboard", "-d", "/workspace", "-c", "all", "--no-serve"]
    volumes: [".:/workspace"]
    working_dir: /workspace
    depends_on:
      scan: { condition: service_completed_successfully }

  dashboard:
    build: { context: ., dockerfile: Dockerfile.nginx }
    image: claudesec-dashboard:local
    ports: ["11777:8080"]
    volumes:
      - ./claudesec-asset-dashboard-live.html:/usr/share/nginx/html/index.html:ro
      - ./claudesec-dashboard.html:/usr/share/nginx/html/scan.html:ro
      - ./.claudesec-assets/dashboard-data.json:/usr/share/nginx/html/.claudesec-assets/dashboard-data.json:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
    restart: unless-stopped
    depends_on:
      dashboard-gen: { condition: service_completed_successfully }
