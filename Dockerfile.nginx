FROM nginx:1.27-alpine
COPY nginx/default.conf /etc/nginx/conf.d/default.conf
# Dashboard HTML is mounted as volume at runtime (excluded from .dockerignore)
RUN echo '<!DOCTYPE html><html><body>Dashboard not mounted. Use docker-compose or -v flag.</body></html>' > /usr/share/nginx/html/index.html
EXPOSE 8080
