# Optional dependencies for integration scripts (asset sync / dashboard build).
# These require external API keys (Google Sheets, Google Drive) and are NOT
# needed for CI test runs. Install only when running scripts/build-dashboard.py
# or scripts/sync-assets.py against live APIs.
#
# Pinned exactly for the same reason as requirements-ci.txt: an unconstrained
# requirement gives GitHub's dependency graph no version to match advisories
# against, so these packages were invisible to Dependabot alerts even though the
# `pip` ecosystem in .github/dependabot.yml already watches this directory.
# Guarded by scanner/tests/test_ci_requirements_pinned.py.
gspread==6.2.1
google-auth==2.56.3
google-auth-oauthlib==1.4.0
openpyxl==3.1.5
requests==2.34.2
